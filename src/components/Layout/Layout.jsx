import { Container } from "@mui/material";

const Layout = ({ children }) => {
  return (
    <div style={{ margin: 0, padding: 0, background: "white" }}>{children}</div>
  );
};
export default Layout;
